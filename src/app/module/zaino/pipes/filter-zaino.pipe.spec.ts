import { FilterZainoPipe } from './filter-zaino.pipe';

describe('FilterZainoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterZainoPipe();
    expect(pipe).toBeTruthy();
  });
});
