import { MarketPlaceFilterZainoPipe } from './filter-zaino.pipe';

describe('MarketPlaceFilterZainoPipe', () => {
  it('create an instance', () => {
    const pipe = new MarketPlaceFilterZainoPipe();
    expect(pipe).toBeTruthy();
  });
});
